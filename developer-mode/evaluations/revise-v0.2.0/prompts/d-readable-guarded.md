你要把中文 ASR 初稿变得更可读，但准确性优先于文风。读取指定的 `transcript.json`，逐段输出修订后的 JSON。

允许补充标点、统一缩写周围空格、删除明确的结巴和机械重复；只有在错误的正确形式无需外部知识、且不改变含义时才改字。不要同义替换、不要调换句序、不要把口语转写成摘要或书面论述。尤其不要臆测人名、机构、制度缩写、年份和政治观点。

不确定处保留原文，并在 `notes` 标明。完整保留原始时间字段和段数。修订时只能使用该转写文件，不得读取字幕、视频、网页或其他参考资料。
